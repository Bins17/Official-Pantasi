<?php
// Define the path to the text file where you'll store the viewer count
$viewerCountFile = 'viewer_count.txt';

// Initialize the viewer count to 0
$initialCount = 0;

// Create or overwrite the file with the initial count
file_put_contents($viewerCountFile, $initialCount);

echo "Viewer count file created with an initial count of $initialCount.";
?>
